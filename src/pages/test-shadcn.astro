---
import Layout from '../layouts/Layout.astro';
import Card from '../lib/components/ui/card.svelte';
import Button from '../lib/components/ui/button.svelte';
import Badge from '../lib/components/ui/badge.svelte';
---

<Layout title="shadcn-svelte Test">
	<div class="container mx-auto p-8 space-y-8">
		<div class="space-y-4">
			<h1 class="text-4xl font-bold">shadcn-svelte Components Test</h1>
			<p class="text-muted-foreground">Testing shadcn-svelte components in Astro</p>
		</div>

		<!-- Buttons -->
		<div class="space-y-4">
			<h2 class="text-2xl font-semibold">Buttons</h2>
			<div class="flex gap-2 flex-wrap">
				<Button client:load>Default</Button>
				<Button client:load variant="secondary">Secondary</Button>
				<Button client:load variant="destructive">Destructive</Button>
				<Button client:load variant="outline">Outline</Button>
				<Button client:load variant="ghost">Ghost</Button>
				<Button client:load variant="link">Link</Button>
			</div>
			<div class="flex gap-2 flex-wrap">
				<Button client:load size="sm">Small</Button>
				<Button client:load>Default</Button>
				<Button client:load size="lg">Large</Button>
			</div>
		</div>

		<!-- Badges -->
		<div class="space-y-4">
			<h2 class="text-2xl font-semibold">Badges</h2>
			<div class="flex gap-2 flex-wrap">
				<Badge client:load>Default</Badge>
				<Badge client:load variant="secondary">Secondary</Badge>
				<Badge client:load variant="destructive">Destructive</Badge>
				<Badge client:load variant="outline">Outline</Badge>
			</div>
		</div>

		<!-- Cards -->
		<div class="space-y-4">
			<h2 class="text-2xl font-semibold">Cards</h2>
			<div class="grid grid-cols-1 md:grid-cols-3 gap-4">
				<Card client:load class="p-6 space-y-2">
					<h3 class="text-lg font-semibold">📦 Склад</h3>
					<p class="text-sm text-muted-foreground">15 товаров на складе</p>
					<Badge client:load variant="secondary">Активен</Badge>
					<div class="pt-4">
						<Button client:load size="sm">Открыть</Button>
					</div>
				</Card>

				<Card client:load class="p-6 space-y-2">
					<h3 class="text-lg font-semibold">🍳 Кухня</h3>
					<p class="text-sm text-muted-foreground">23 товара на кухне</p>
					<Badge client:load variant="destructive">Требует заказа</Badge>
					<div class="pt-4">
						<Button client:load size="sm">Открыть</Button>
					</div>
				</Card>

				<Card client:load class="p-6 space-y-2">
					<h3 class="text-lg font-semibold">🍷 Бар</h3>
					<p class="text-sm text-muted-foreground">18 товаров в баре</p>
					<Badge client:load>Нормально</Badge>
					<div class="pt-4">
						<Button client:load size="sm">Открыть</Button>
					</div>
				</Card>
			</div>
		</div>

		<!-- Order Card Example -->
		<div class="space-y-4">
			<h2 class="text-2xl font-semibold">Order Card Example</h2>
			<Card client:load class="p-6 space-y-4">
				<div class="flex justify-between items-start">
					<div>
						<h3 class="text-xl font-semibold">🏪 Склад #1234</h3>
						<p class="text-sm text-muted-foreground">15 января 2025, 14:30</p>
					</div>
					<Badge client:load variant="secondary">Ожидает</Badge>
				</div>

				<div class="grid grid-cols-3 gap-4">
					<div>
						<p class="text-sm text-muted-foreground">Товаров</p>
						<p class="text-2xl font-bold">12</p>
					</div>
					<div>
						<p class="text-sm text-muted-foreground">Единиц</p>
						<p class="text-2xl font-bold">45</p>
					</div>
					<div>
						<p class="text-sm text-muted-foreground">Категорий</p>
						<p class="text-2xl font-bold">3</p>
					</div>
				</div>

				<div class="space-y-2">
					<div class="border rounded-lg p-3 space-y-2">
						<div class="flex justify-between items-center">
							<h4 class="font-semibold text-sm">🏷️ Молочные продукты</h4>
							<span class="text-xs text-muted-foreground">📞 Поставщик А</span>
						</div>
						<div class="space-y-1">
							<div class="flex justify-between text-sm">
								<span>Молоко</span>
								<Badge client:load variant="outline">10 л</Badge>
							</div>
							<div class="flex justify-between text-sm">
								<span>Сметана</span>
								<Badge client:load variant="outline">5 кг</Badge>
							</div>
						</div>
					</div>
				</div>

				<div class="flex justify-end gap-2">
					<Button client:load variant="outline" size="sm">👁️ Детали</Button>
					<Button client:load size="sm">📤 Отправить</Button>
				</div>
			</Card>
		</div>

		<div class="pt-8">
			<a href="/manager" class="text-sm text-muted-foreground hover:underline">
				← Back to Manager
			</a>
		</div>
	</div>
</Layout>
